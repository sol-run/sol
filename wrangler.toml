name = "sol-run-website"
compatibility_date = "2025-05-21"
compatibility_flags = ["nodejs_compat"]

# Remove the build section as it's handled by Cloudflare Pages
# Remove the [build.upload] section as it's not needed

[site]
bucket = ".next"
# Remove entry-point as it's not needed for Pages deployments
