name = "sol-run-website"
compatibility_date = "2025-05-21"

[build]
command = "pnpm run build"

[site]
bucket = ".next"

[env.production]
routes = [
  { pattern = "/*", script = "index.js" }
]

# Specify the correct output format for Cloudflare Pages
[build.upload]
format = "directory"
